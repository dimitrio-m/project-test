extends layout
block content
  ol.breadcrumb
    li.active Inicio
  .jumbotron
    h1=group
    p.lead Este grupo es de personas apasionadas, puedes leer todos los post de los usuarios sobre experiencias nuevas o sobre problemas que hán tenido y comentarlos.
    p
      a.btn.btn-lg.btn-success(href=`/${group}/create_post`, role='button') Crear Post
  .row.marketing
    each category in categories
      .col-lg-6
        .panel.panel-default
          .panel-body
            h4
              a(href=`/${group}/${category}`)= category
